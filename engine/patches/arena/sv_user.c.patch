--- sv_user.c
+++ sv_user.c
@@ -621,6 +621,10 @@
 			continue;
 		}
 
+		/* Grouped stdin actions override any stale observer-client command after
+		   network draining and immediately before movement physics. */
+		QNN_ArenaApplyStagedClient (host_client);
+
 // always pause in single player if in console or menus
 		if (!sv.paused && (svs.maxclients > 1 || key_dest == key_game) )
 			SV_ClientThink ();
