--- net_main.c
+++ net_main.c
@@ -398,6 +398,10 @@
 		}
 	}
 
+	/* Grouped PPO clients always receive an explicit loopback address. */
+	if (host && COM_CheckParm("-qnn_direct_connect"))
+		goto JustDoIt;
+
 	slistSilent = host ? true : false;
 	NET_Slist_f ();
 
@@ -833,6 +837,8 @@
 	}
 
 	net_numsockets = svs.maxclientslimit;
+	if (COM_CheckParm("-qnn_virtual_clients"))
+		net_numsockets = svs.maxclientslimit * 2 + 1;
 	if (cls.state != ca_dedicated)
 		net_numsockets++;
 
